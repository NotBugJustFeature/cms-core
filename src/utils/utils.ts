import clc from 'cli-color'

export var errorColor = clc.red
export var errorBoldColor = clc.red.bold
export var warnColor = clc.yellow
export var okColor = clc.green
export var noticeColor = clc.blue
